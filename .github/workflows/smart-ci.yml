name: Smart CI

on:
  workflow_call:

jobs:

  checkout-test:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - name: List Files
        run: |
          pwd
          ls -la